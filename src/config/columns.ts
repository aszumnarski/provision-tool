import * as f from "./fields";

export const modes = { fields: [f.mode] };
export const appNumber = { fields: [] };
export const stateControllerButton = { fields: [] };

export const one = { fields: [] };
export const two = { fields: [] };
export const three = { fields: [] };

export const four = { fields: [] ,header: "pierwsza kolumna", };
export const five = { fields: [],header: "druga kolumna", };
export const six = { fields: [] ,header: "trzecia kolumna",};
